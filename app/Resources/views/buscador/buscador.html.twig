{{ form_start(buscadorForm) }}
<div class="row">
    <div class="col-md-9 col-12">{{ form_widget(buscadorForm) }}</div>
    <div class="col-md-3 col-12 mt-1">
        <div class="align-center mt-4">
            <input type="submit" class="btn btn-primary w-75 btn-sm mt-2" value="Filtrar"/>
        </div>
    </div>
</div>
{{ form_end(buscadorForm) }}